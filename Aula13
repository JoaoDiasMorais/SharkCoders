from random import randint
def lista():
    x = [randint(1 , 20) for y in range(10)]
    print(x)
def somar():

    y = [num for num in x if num % 2 == 0]
    total = sum(y)
    print(total)

lista()
somar()
